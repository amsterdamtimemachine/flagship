### sveltekit app ###
PRIVATE_VISUALIZATION_BINARY_PATH=../../data/visualization.bin # don't change this
# MapTiler API key - get from https://www.maptiler.com/
PUBLIC_MAPTILER_API_KEY=your_maptiler_api_key_here

### preprocessor script ###
OUTPUT_PATH=../../data/visualization.bin # don't change this

# Database configuration (optional - uses defaults if not set)
# DATABASE_BASE_URL=https://atmbackend.create.humanities.uva.nl
# DATABASE_BATCH_SIZE=400
# DATABASE_TIMEOUT=30000

# Geographic bounds (optional - Amsterdam bounds if not set)
# BOUNDS_MIN_LON=4.85
# BOUNDS_MAX_LON=4.95
# BOUNDS_MIN_LAT=52.35
# BOUNDS_MAX_LAT=52.4

# Grid configuration (optional - uses defaults if not set)
# GRID_COLS=20
# GRID_ROWS=20
# GRID_PADDING=0.05

# Chunking configuration (optional - uses defaults if not set)
# CHUNK_ROWS=4
# CHUNK_COLS=4
# CHUNK_OVERLAP=0.001
# CHUNK_DELAY_MS=75

